const Bag = require('./Bag')

describe('Bag class', () => {
    //bag has weight
    test('bag has weight', () => {

    })
    //if bag has no weight, return error
    test('bag has no weight to return an error', () => {
        
    })
})